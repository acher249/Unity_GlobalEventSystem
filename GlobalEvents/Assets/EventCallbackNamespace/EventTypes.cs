namespace EventCallbacks
{
    // we will have a dictionary that will be organized based on the type of event
    // event types that we can fire
    public enum EVENT_TYPE {

        UNIT_SPAWNED,
        BULLET_SHOT,
        LOOT_DROPPED,
        UNIT_DIED
    }
}